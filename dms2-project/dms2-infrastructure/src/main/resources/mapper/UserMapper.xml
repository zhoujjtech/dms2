<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dms2.infrastructure.repository.mapper.UserMapper">

  <!-- 结果映射 -->
  <resultMap id="BaseResultMap" type="com.example.dms2.domain.model.entity.User">
    <id column="id" property="id" jdbcType="BIGINT"/>
    <result column="username" property="username" jdbcType="VARCHAR"/>
    <result column="email" property="email" jdbcType="VARCHAR"/>
    <result column="phone" property="phone" jdbcType="VARCHAR"/>
    <result column="real_name" property="realName" jdbcType="VARCHAR"/>
    <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
    <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
  </resultMap>

  <!-- 基础列 -->
  <sql id="Base_Column_List">
    id, username, email, phone, real_name, create_time, update_time
  </sql>

  <!-- 根据 ID 查询用户 -->
  <select id="selectById" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List"/>
    FROM t_user
    WHERE id = #{id}
  </select>

  <!-- 根据用户名查询用户 -->
  <select id="selectByUsername" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List"/>
    FROM t_user
    WHERE username = #{username}
  </select>

  <!-- 根据邮箱查询用户 -->
  <select id="selectByEmail" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List"/>
    FROM t_user
    WHERE email = #{email}
  </select>

  <!-- 查询所有用户 -->
  <select id="selectAll" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List"/>
    FROM t_user
    ORDER BY create_time DESC
  </select>

  <!-- 根据 ID 列表查询用户 -->
  <select id="selectByIds" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List"/>
    FROM t_user
    WHERE id IN
    <foreach collection="ids" item="id" open="(" separator="," close=")">
      #{id}
    </foreach>
    ORDER BY create_time DESC
  </select>

  <!-- 插入用户 -->
  <insert id="insert" parameterType="com.example.dms2.domain.model.entity.User"
          useGeneratedKeys="true" keyProperty="id" keyColumn="id">
    INSERT INTO t_user (username, email, phone, real_name, create_time, update_time)
    VALUES (#{username}, #{email}, #{phone}, #{realName}, #{createTime}, #{updateTime})
  </insert>

  <!-- 根据 ID 更新用户 -->
  <update id="updateById" parameterType="com.example.dms2.domain.model.entity.User">
    UPDATE t_user
    SET username = #{username},
        email = #{email},
        phone = #{phone},
        real_name = #{realName},
        update_time = #{updateTime}
    WHERE id = #{id}
  </update>

  <!-- 根据 ID 删除用户 -->
  <delete id="deleteById">
    DELETE FROM t_user
    WHERE id = #{id}
  </delete>

  <!-- 统计用户名数量 -->
  <select id="countByUsername" resultType="int">
    SELECT COUNT(*)
    FROM t_user
    WHERE username = #{username}
  </select>

  <!-- 统计邮箱数量 -->
  <select id="countByEmail" resultType="int">
    SELECT COUNT(*)
    FROM t_user
    WHERE email = #{email}
  </select>

  <!-- 分页查询用户 -->
  <select id="selectByPage" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List"/>
    FROM t_user
    ORDER BY create_time DESC
    LIMIT #{offset}, #{limit}
  </select>

</mapper>
